<?php

/**
 * Implements hook_theme()
 * @return mixed
 */
function recent_trends_theme($existing, $type, $theme, $path) {

  $variables = array(
    'recent_trends_template' => array(
      'variables' => array(
      ),
      'template' => 'block--recent_trends',
    ),
  );
  return $variables;
}
